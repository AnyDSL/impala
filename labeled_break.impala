fn main(n: int) -> int {
    for (let mut i = 0; i < n; ++i) {
        let outer = || { break; };

        for (let mut j = 0; j < n; ++j) {
            if (i == j)
                outer();
        }
    }

    return 42;
}
