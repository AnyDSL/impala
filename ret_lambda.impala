def f(a : int) -> pi(int) -> int {
    return lambda(i : int) -> int { 
        if (i == a)
            return i + a; 
        else
            return i + 42;
    };
}

def main(i : int) -> int {
    return f(i)(i);
}
