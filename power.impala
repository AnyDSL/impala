fn power(a: int, mut b: int) -> int {
    let mut result = 1;
    while (b != 0) {
        result *= a;
        --b;
    }

    return result;
}

fn main(i: int) -> int {
    //let power4 = |a: int| -> int { return @power(4, a); };
    //return power4(i) + power(3, 4);
    return @power(3, 4);
}
