fn range(a: int, b: int, body: fn(int) -> ()) -> () {
    if a < b {
        body(a);
        range(a+1, b, body)
    }
}

fn main(a: int, b: int) -> () {
    for i in range(a, b) {
    }
    for j in range(a, b) {
    }
}
