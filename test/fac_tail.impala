extern fn fac(n : int) -> int {
    if (n == 0) 
        return 1;
    if (n == 1) 
        return 1;

    return helper(1, 2, n);
}

fn helper(r : int, i : int, n : int) -> int {
    if (i <= n)
        return helper(r * i, i + 1, n);
    else
        return r;
}
