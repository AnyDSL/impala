fn main(n: int) -> int {
    return @loop(n);
}

fn loop(n: int) -> int {
    for (let mut i: int = 0; i != n; ++i) {}
    let mut j = 0;
    for (let mut i: int = 0; i != 42; ++i) { ++j; }
    return j;
}
