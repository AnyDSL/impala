fn foo(a: int, b: int) -> int {
    let mut res1 = 0;
    for (let mut i = 0; i < a; ++i)
        res1 += 666;

    let mut res2 = 0;
    for (let mut i = 0; i < b; ++i)
        res2 += 3333;

    return 17 + res2;
}

fn main(i: int) -> int {
    return @foo(i, 10);
}
