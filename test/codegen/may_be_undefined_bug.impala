fn iterate_children(mut node_id: &i32) -> () {
    for call_me() {
        if 42 < *node_id {
            // Nothing to see here
        }
    }
    *node_id = 42;
}

fn call_me(body: fn() -> ()) -> () {
    body();
}

fn main() -> () {
    let mut node_id = 42;
    while node_id < 42 {
        @iterate_children(&node_id);
    }
}
