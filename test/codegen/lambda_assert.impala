fn iteration_sep_advanced() -> () {
    init_zero(~[23: f32]);
}

fn range(body: fn(i32) -> ()) -> () {
    range(body, return)
}

fn init_zero(mut arr: &[f32]) -> () {
    for i in range() {
        arr(i) = 1337f;
    }
}

fn main() -> () {
    init_zero(~[42: f32]);
}
