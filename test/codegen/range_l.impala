fn range(a: int, b: int, body: fn(int)->()) -> () {
    if a < b {
        body(a);
        range(a+1, b, body, return)
    } else {
        return()
    }
}

fn main() -> i32 {
    let mut sum = 0;
    
    range(0, 10, |i| sum += i);
    sum
}
