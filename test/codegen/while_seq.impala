// codegen

extern fn f(n: int) -> int {
    let mut sum = 0;
    while sum < n {
        ++sum;
    }
    while sum < n {
        ++sum;
    }
    sum
}

fn main() -> int {
    if f(5) == 5 { 0 } else { 1 }
}
