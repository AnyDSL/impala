// codegen

fn main() -> int {
    let mut sum = 0;
    while true {
        ++sum;
        if sum == 1 {
            continue()
        } else if sum == 2 {
            break()
        }
        sum += 3
    }
    if sum == 2 { 0 } else { 1 }
}
