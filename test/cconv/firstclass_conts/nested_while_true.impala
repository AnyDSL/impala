extern "C" { fn do_something(i32) -> (); }

extern fn outer(x:i32) -> i32 {
    let ret = return;
    fn inner(y:i32) -> i32 {
        if y > 0 { ret(x + y - 1337) } else { x + y }
    }
    inner(x - 42) * 2;
    let mut i = 0;
    while(true) {
        do_something(++i)
    }
    42
}
