fn combine(i : int, x : int) -> int {
    return i + x;
}

fn main(xn : int) -> int {
    let mut result : int = 0;
    for (let mut x : int = 0; x < xn; ++x)
        result = combine(result, x);

    return result;
}
