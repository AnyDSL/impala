extern fn fac(n: int) -> int {
    fn helper(r: int, i : int) -> int {
        if i <= n {
            helper(r * i, i + 1)
        } else {
            r
        }
    }

    if n <= 1 {
        1;
    }

    helper(1, 2)
}
