def extern head(ir : sigma(int, int)) -> sigma(int, int, bool) {
    i : int = ir[0];
    r : int = ir[1];
    n : int = 42;

    if (r == 17)
        return (#(i, r, i <= n));
    else
        return (#(i, r, i <= n));
}
