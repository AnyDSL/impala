fn extern head(ir : sigma(int, int)) -> sigma(int, int, bool) {
    var i : int = ir[0];
    var r : int = ir[1];
    var n : int = 42;

    if (r == 17)
        return (#(i, r, i <= n));
    else
        return (#(i, r, i <= n));
}
