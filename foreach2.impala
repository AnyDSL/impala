/*
foreach without float
*/
def count(n : float, yield : pi(float, pi()), ret : pi()) {
    def loop(i : float) {
        if (i < n)
            yield(i, lambda() { loop(i+1.0f); });
        else
            ret();
    }
    loop(0.0f);
}

def main() -> float {
    b : float = 2.0f;
    foreach (x : float  <- count(10.0f)) {
        b += x;
    }

    x : float = 42.0f;
    foreach (x  <- count(10.0f)) {
        b += x;
    }

    return b;
}
