def extern f(i : int) -> int {
    
    b : int = 7;

    def g(j : int) -> int { return h(i + j); }

    def h(j : int) -> int { return f(g(j+b)); }

    return h(23);
}

def foo(i : int) -> int { return i; }
